import { CSSProperties } from "react";

export interface IAppThemeTypography {
    50: CSSProperties;
    100: CSSProperties;
    175: CSSProperties;
    200: CSSProperties;
    300: CSSProperties;
    400: CSSProperties;
    700: CSSProperties;
}
